CREATE TABLE IF NOT EXISTS channelcountchannel(
    guildid VARCHAR(18) NOT NULL,
    channelid VARCHAR(18) NOT NULL
);

CREATE TABLE IF NOT EXISTS membercountchannel(
    guildid VARCHAR(18) NOT NULL,
    channelid VARCHAR(18) NOT NULL
);

CREATE TABLE IF NOT EXISTS datechannel(
    guildid VARCHAR(18) NOT NULL,
    channelid VARCHAR(18) NOT NULL
);


